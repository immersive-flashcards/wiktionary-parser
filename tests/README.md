# Testing Strategy

**TL;DR:** Run regression tests with the default config for all languages using:
```shell
python -m unittest discover -s tests
```

---

 - The test configuration can be found in `config/runs/test.yml`.